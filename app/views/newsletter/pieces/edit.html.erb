<% title "Editing #{ @piece.element.name }" %>
<p><%= @piece.element.description %></p>
<%= render :partial => 'form' %>


<%= content_for :page_navigation do %>
  <div>
    <%= link_to 'Back', edit_newsletter_path(@newsletter), :class => 'button' %>
  </div>
<% end %>

